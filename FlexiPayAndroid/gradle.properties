# Increase Gradle daemon heap size (e.g., 4GB)
# Adjust based on your system's available RAM
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# Enable AndroidX libraries (required for Jetpack Compose and recent libraries)
android.useAndroidX=true

# Enable Gradle configuration caching (optional, can speed up builds)
# org.gradle.configuration-cache=true

# Enable parallel builds (optional, uses more cores)
# org.gradle.parallel=true 