<?xml version="1.0" encoding="utf-8"?>
<!--
   Sample data extraction rules file; uncomment sections to enable.
   See https://developer.android.com/about/versions/12/backup-restore#xml-changes
   for details.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Implement rule to include/exclude files for cloud backup -->
        <!-- <include domain="sharedpref" path="."/> -->
        <!-- <exclude domain="sharedpref" path="device.xml"/> -->
        <!-- Exclude everything by default -->
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <!-- TODO: Implement rule to include/exclude files for device-to-device transfer -->
        <!-- <include domain="sharedpref" path="."/> -->
        <!-- <exclude domain="sharedpref" path="device.xml"/> -->
        <!-- Exclude everything by default -->
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules> 